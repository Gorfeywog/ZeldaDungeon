using System;
using System.Collections.Generic;
using System.Text;

/*
namespace ZeldaDungeon.Entities
{
    class Collision
    {
        public enum CollisionDirection{top, bottom, left, right, none};

        List<IEntity> = parseForProximity(allObjects);

        parseForProximity(IList<IEntity> allObjects)
        {
            foreach (IEntity entity in allObjects){
                if (entity.)
            }
        }
        // Move through blue floor blocks and blue sand blocks
        // Check object against all objects in similar x-position
        // for each object in similar x-position, check for y-position within object square.
    }
}
*/